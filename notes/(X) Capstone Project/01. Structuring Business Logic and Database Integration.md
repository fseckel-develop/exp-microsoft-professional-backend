## Overview

- Development of the **Order Management System** for `LogiTrack`
- Focus on managing:
	- Inventory items  
	- Customer orders  
	- Multiple fulfilment centers  
- Foundation for upcoming activities:
	- API endpoints  
	- Secure access  
	- Performance optimisation  
- Core goals:
	- Create business logic classes  
	- Integrate relational database with EF Core  
	- Implement basic CRUD logic  
	- Use Microsoft Copilot for optimisation  

---
## Core Programming Concepts (C# & OOP)

- C# as the primary programming language  
- Variables:
	- Store values like quantity or location  
- Methods:
	- Define behaviours (e.g., printing, updating orders)  
- Classes:
	- Group related properties and methods  
	- Model real-world entities  
- Object-Oriented Programming (OOP):
	- Breaks complex systems into manageable parts  
	- Organises logic using classes and objects  
	- Improves readability and extensibility  

---
## Project Setup

- Create project via CLI:
	- `dotnet new webapi -n LogiTrack`  
	- `cd LogiTrack`  
- Create `Models` folder  
- Add class files:
	- `InventoryItem.cs`  
	- `Order.cs`  

---
## InventoryItem Class

- Create a class named `InventoryItem` with the following properties:
	- ItemId (int)
	- Name (string)
	- Quantity (int)
	- Location (string)

- Include a method named `DisplayInfo()` that prints: 
	  "Item: Pallet Jack | Quantity: 12 | Location: Warehouse A"
- Test this method with a sample object in Program.cs or a test controller.

---
## Order Class

- Create a class named `Order` with the following properties:
	- OrderId (int)
	- CustomerName (string)
	- DatePlaced (DateTime)
	- List (list of items in the order)

- Add methods to:
	- `AddItem(InventoryItem item)`
	- `RemoveItem(int itemId)`
	- `GetOrderSummary()`
	  returns a string like: "Order #1001 for Samir | Items: 2 | Placed: 4/5/2025"
- Write a test block to create an order, add/remove items, and print the summary

---
## Database Integration with Entity Framework Core

- Add EF Core packages via NuGet or CLI: 
	  `dotnet add package Microsoft.EntityFrameworkCore.Sqlite`
	  `dotnet add package Microsoft.EntityFrameworkCore.Tools`

- Create a new `DbContext` class in a file named `LogiTrackContext.cs`:

```csharp
using Microsoft.EntityFrameworkCore;

public class LogiTrackContext : DbContext
{
    public DbSet<InventoryItem> InventoryItems { get; set; }
    public DbSet<Order> Orders { get; set; }

    protected override void OnConfiguring(DbContextOptionsBuilder options)
        => options.UseSqlite("Data Source=logitrack.db");
}
```

- Use proper Data Annotations where needed:
	- `[Key]`
	- `[Required]`
	- `[ForeignKey]`

- Use migrations to create your database:
	  `dotnet ef migrations add InitialCreate`
	  `dotnet ef database update`

---
## Seeding and Testing the Database

- Open Program.cs (or your test controller) and add the following block of code:

```csharp
using (var context = new LogiTrackContext())
{
    // Add test inventory item if none exist
    if (!context.InventoryItems.Any())
    {
        context.InventoryItems.Add(new InventoryItem
        {
            Name = "Pallet Jack",
            Quantity = 12,
            Location = "Warehouse A"
        });
        context.SaveChanges();
    }

    // Retrieve and print inventory to confirm
    var items = context.InventoryItems.ToList();
    foreach (var item in items)
    {
        item.DisplayInfo(); 
        // Should print: Item: Pallet Jack | Quantity: 12 | Location: Warehouse A
    }
}
```

- Run the application to confirm that the data is saved and printed as expected.

- This step ensures that your ...
	- DbContext is working
	- tables are populated
	- `DisplayInfo()` method works correctly

---
## One-to-Many Relationship Concept

- One `Order` can contain many `InventoryItems` 
- Configuration options:
	- Data Annotations  
	- Fluent API  
- Ensures:
	- Proper relational structure  
	- Scalable backend architecture  

But: Can one `InventoryItem` be part of many `Order` instances?

---
## Using Microsoft Copilot for Optimisation

- Sample prompts:
	- Generate EF Core code for one-to-many between `Order` and `InventoryItem`
	- Suggest a way to print Order summaries efficiently  
	- Identify performance improvements in `AddItem` method  
- After review:
	- Refactor logic  
	- Improve performance  
	- Ensure clean compilation  

---
## Submission Checklist

- Two working classes:
	- `InventoryItem`
	- `Order`
- Test data with visible output  
- `LogiTrackContext` integrated with EF Core  
- Database created and tested  
- Copilot suggestions reviewed and applied  
- Code prepared for Part 2 expansion  

---